#include <stdio.h>

// 把出现PI的位置全部替换成3.1415926，不管对错合理与否，也不会在预处理阶段报错，单纯替换。
#define PI 3.1415926

int main(int argc, char **argv)
{
    PI = 3;
    PI();
    get PI;
    PI

    return 0;
}

PI PI PI